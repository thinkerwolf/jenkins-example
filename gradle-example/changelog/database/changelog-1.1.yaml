databaseChangeLog:
  - changeSet:
      id: 1.1.0
      author: henry.hua
      labels: example-label
      context: example-context
      comment: example-comment
      changes:
        - sql:
            sql: |-
              USE test;
              DELETE FROM company WHERE id > 100;
